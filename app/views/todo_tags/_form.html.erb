<%= form_with(model: todo_tag, local: true) do |form| %>
  <% if todo_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_tag.errors.count, "error") %> prohibited this todo_tag from being saved:</h2>

      <ul>
      <% todo_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
